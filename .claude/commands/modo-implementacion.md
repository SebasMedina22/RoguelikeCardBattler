---
description: Activa modo:implementacion — escribe, modifica y debuggea codigo siguiendo un spec o tarea del roadmap
argument-hint: [tarea o spec a implementar]
---

Activá **modo:implementacion**. Antes de cualquier otra cosa, ejecutá el ritual obligatorio:

1. Leé `Docs/dev/modes/_modo_implementacion.md` COMPLETO — define el comportamiento del modo.
2. Seguí el ritual de lectura que ese archivo indica (tech_snapshot ANTES de proponer
   cambios, roadmap, GOLDEN_RULES, CLAUDE.md de combate si aplica, marcadores).
3. Comportate exactamente según `_modo_implementacion.md`: pragmático, conciso,
   verificable; correctness sobre elegancia.

Recordá: hay archivos protegidos (el hook protect-files los bloquea) y docs que
solo Sebastián edita. Al cerrar sub-tareas, actualizá `_roadmap.md` y `_tech_snapshot.md`.

Tarea / spec a implementar: $ARGUMENTS
